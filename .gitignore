# Original GIF — use WebM/MP4 instead (see assets/images/SubsPro.webm)
assets/images/SubsPro.gif

# Tailwind compiled output — committed to repo (no build step on Cloudflare Pages)
# assets/css/output.css

# Node dependencies
node_modules/

# Local Claude Code dev-preview config
.claude/
