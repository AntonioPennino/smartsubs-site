# Netlify Configuration for SmartSubs.pro

[build]
  publish = "."
  # Compile Tailwind (output.css is gitignored and generated here at deploy time)
  command = "npm install && npm run build:css"

# ─── Security & Performance Headers (all routes) ───────────────────────────
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    Content-Security-Policy = "upgrade-insecure-requests"
    # Tells browsers to always use HTTPS for 1 year
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"

# ─── Cache: HTML — short TTL so deploys propagate fast ─────────────────────
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# ─── Cache: CSS & JS — long TTL (Netlify busts cache on deploy) ────────────
[[headers]]
  for = "/styles.css"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/tokens/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/assets/css/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/assets/js/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# ─── Cache: Images & SVGs ───────────────────────────────────────────────────
[[headers]]
  for = "/assets/images/*.svg"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/assets/images/*.png"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/assets/images/*.mp4"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# ─── Redirects ──────────────────────────────────────────────────────────────
# Redirect www → apex domain (set Primary Domain in Netlify DNS too)
[[redirects]]
  from = "https://www.smartsubs.pro/*"
  to   = "https://smartsubs.pro/:splat"
  status = 301
  force  = true

# Catch-all → proper 404 (NOT a 200 soft 404)
[[redirects]]
  from   = "/*"
  to     = "/404.html"
  status = 404
